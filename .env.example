# A Resend API key with sending permission; configure for Vercel Production and previews.
RESEND_API_KEY=
# Sender address on a domain verified in Resend (not a visitor's email).
CONTACT_FROM_EMAIL="Portfolio <contact@yourdomain.com>"
